import styled from "styled-components";

export const StyledSimilarMoviesContainer = styled.section`
  margin-top: 140px;

  h2 {
    font-size: var(--sz-title-2);
    font-weight: var(--font-weg-2);
  }
`;
